[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cstlab"
version = "0.0.0"
authors = [{name="Your Name"}]
description = "Carry‑Symmetric Topology reference implementation"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
